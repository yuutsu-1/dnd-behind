[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests that hit a real Postgres database (backend/tests/integration), excluded from the default unit run via `-m "not integration"`
